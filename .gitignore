# dependencies
node_modules/
.expo/
.expo-shared/

# native builds
android/
ios/
*.apk
*.ipa
build/
dist/

# models (too large to commit — fetched per data/README)
*.gguf
models/

# datasets — raw dumps AND the generated DB (~18MB). Regenerate with: npm run data
data/raw/
data/pharos.db

# local env & secrets
.env
.env.*

# logs & captures from local runs (keep curated evidence elsewhere)
*.log
*.pcap

# OS
.DS_Store
Thumbs.db
